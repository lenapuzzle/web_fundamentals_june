// change Login to Logout onclick
function changeElement(element) {
  element.innerText = "Logout";
}

// remove Add Definition button onclick
function removeElement(element) {
  element.remove();
}

